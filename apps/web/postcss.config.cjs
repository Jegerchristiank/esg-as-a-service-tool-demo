/**
 * Minimal PostCSS configuration for the Next.js app.
 * Using CommonJS avoids Next treating the export as an ES module and triggering
 * the unsupported `__esModule` field warning.
 */
module.exports = {
  plugins: []
}
